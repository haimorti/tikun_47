{
  "scripts": {
    "start": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -r src dist/src"
  },
  "dependencies": {},
  "devDependencies": {}
}
